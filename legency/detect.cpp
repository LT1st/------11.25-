/* #include "detect.h"

bool CheckBallOrder(Mat roi_init)
{
    row_CheckBallOrder = roi_init.rows;
    col_CheckBallOrder = roi_init.cols;

    //for()


    return 0;//这次没找到

    return 1;//找到了
} */